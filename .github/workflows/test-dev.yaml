name: Test DEV Workspace Access

on:
  workflow_dispatch:

jobs:
  test-dev-access:
    runs-on: ubuntu-latest
    steps:
      - name: Install jq
        run: sudo apt-get install -y jq

      - name: List All Workspaces
        run: |
          TOKEN=$(curl -s -X POST \
            https://login.microsoftonline.com/${{ secrets.TENANT_ID }}/oauth2/v2.0/token \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "client_id=${{ secrets.CLIENT_ID }}&client_secret=${{ secrets.CLIENT_SECRET }}&grant_type=client_credentials&scope=https://analysis.windows.net/powerbi/api/.default" | jq -r '.access_token')

          echo "=== All workspaces SP can see ==="
          curl -s -X GET "https://api.powerbi.com/v1.0/myorg/groups" \
            -H "Authorization: Bearer $TOKEN" | jq '.value[] | {name: .name, id: .id}'